# -- Label used by Grafana's dashboards sidecar to identify config maps with
# dashboards that should be added to Grafana. Should match the value of
# the `sidecar.dashboards.label` configuration in the Grafana chart.
label: grafana_dashboard

# -- Label value used by Grafana's dashboards sidecar to identify config maps
# with dashboards that should be added to Grafana. Should match the value of
# the `sidecar.dashboards.labelValue` configuration in the Grafana chart.
labelValue: ""
